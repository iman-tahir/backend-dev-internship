# Task 1: Node.js and NPM Basics

Verified Node.js and NPM installation, explored the Node.js event loop with a live setTimeout example, used the built-in `fs` module to read a file asynchronously via `require()`, and went through the NPM workflow: 
`npm init -y` and `npm install express`.

## Key files
- `test.js` — demonstrates the event loop (sync code runs before async callbacks)
- `read.js` — reads a file using the built-in `fs` module